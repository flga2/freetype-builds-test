language: C
addons:
  apt:
    packages:
    - bzr
    - procps
    - g++
    - gcc
    - libc6-dev
    - make
    - pkg-config
compiler: gcc
before_script: sudo apt-get remove -y --auto-remove zlib-bin zlib1g zlib1g-dev
script: make dist-linux